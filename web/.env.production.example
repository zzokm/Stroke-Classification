# Production — browser calls same-origin /api; Next.js proxies to the API container.
NEXT_PUBLIC_API_URL=/api

# Server-side proxy target for /api/* route handler (docker compose service name)
INTERNAL_API_URL=http://api:8003
